server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
#      服务注册
      discovery:
        server-addr: localhost:8848

#      服务配置
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: DEV-GROUP  # 分组